// Business Logic

//this doesnt work yet
function convertToPigLatin(inputText){
  const textArray = inputText.split("");
  const newText = []
  for (let index = 1; index < array.length; index +=1) {
    newText.push(textArray[index]);
    console.log(index);
  }
  return newText;
}

//UI Logic

function handleFormSubmission(event) {

}